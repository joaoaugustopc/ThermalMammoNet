services:
  thermal-img-experiment:
    image: fix_ufpe_images
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/experiment
    container_name: experimento-redes-neurais
    deploy:
      resources:
        reservations:
          devices:
            - capabilities: [gpu]

